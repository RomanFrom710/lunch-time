// Here are the common components for selecting and showing
// some point on the map. The map for all places isn't here,
// because it has much cafe-related logic.
export * from './point.model';
export * from './select-point';
